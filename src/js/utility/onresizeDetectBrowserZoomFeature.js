// onresizeDetectBrowserZoomFeature.js

// detect when the person uses browser zoom feature

/*
function whenWindowIsResized()
{
    window.onresize = function()
    {
        // cl(Math.round(window.devicePixelRatio * 100));

        let theZoomLevel = Math.round(window.devicePixelRatio * 100);

        if (theZoomLevel > 150)
        {
            ge("menuMenusId").style.visibility = "hidden";

            ge("menuDropDownScenes").style.visibility = "hidden";

            //ge("theDescription").style.visibility = "hidden";

            if (ge("mousePositionShowId"))
            {
                ge("mousePositionShowId").style.visibility = "hidden";
            }
        }
        else if (theZoomLevel <= 150)
        {
            ge("menuMenusId").style.visibility = "visible";

            ge("menuDropDownScenes").style.visibility = "visible";

            //ge("theDescription").style.visibility = "visible";

            if (ge("mousePositionShowId"))
            {
                ge("mousePositionShowId").style.visibility = "visible";
            }
        }
    };
}
*/

//----//

// Dedicated to God the Father
// All Rights Reserved Christopher Andrew Topalian Copyright 2000-2026
// https://github.com/ChristopherTopalian
// https://github.com/ChristopherAndrewTopalian
// https://sites.google.com/view/CollegeOfScripting

